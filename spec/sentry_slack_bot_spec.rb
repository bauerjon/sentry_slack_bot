require 'spec_helper'

describe SentrySlackBot do
  it 'has a version number' do
    expect(SentrySlackBot::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
